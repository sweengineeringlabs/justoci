# Unicode strings throughout — description, annotations, and config
# values exercise non-ASCII characters that JCS handles via specific
# escape rules. Non-ASCII codepoints in JCS output are emitted as
# raw UTF-8 bytes (NOT as \uXXXX escapes) per RFC 8785; control
# characters (U+0000..U+001F) are the only mandatory \u escapes.

spec_version = "0"
id           = "unicode-fixture:1"
kind         = "raw_image"
description  = "rocket emoji: 🚀, CJK: 日本語, RTL: שלום, combining: é (e + U+0301)"

[[layers]]
source     = "layer-files/blob.bin"
media_type = "application/octet-stream"

[config]
"emoji_key_🚀" = "value with rocket"
"plain_key"    = "value: 中文 + español + русский"

[annotations]
"org.opencontainers.image.description" = "🚀 launch — 日本語 — שלום"
"org.opencontainers.image.title"       = "café"
"org.opencontainers.image.version"     = "1.0"
"vendor.note.combining"                = "naïve vs naïve"
